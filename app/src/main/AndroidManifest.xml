<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:label="SentryReplayRepro"
        android:theme="@android:style/Theme.Material.Light">

        <!--
            Disable Sentry auto-init so we can call SentryAndroid.init() manually in App.kt.
            This mirrors the customer's setup exactly.
        -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ContentActivity" android:exported="false" />
        <activity android:name=".DebugActivity" android:exported="false" />

    </application>

</manifest>
